[vpns]
__sql__ = "SELECT vpns.id, CONCAT('Вирт.узел: ', routers.host_name) FROM vpns INNER JOIN routers ON vpns.router_id = routers.id ORDER BY vpns.id"

[vpn_types]
__sql__ = "SELECT id, vpn_type FROM vpn_types ORDER BY id"
